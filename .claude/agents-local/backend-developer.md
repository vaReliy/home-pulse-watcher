## Extends .claude/agents/backend-developer.md § "Pre-flight"

Also read `rules/local/code-style.md`, `rules/local/testing.md`, `rules/local/validation-authorization.md`, and `rules/local/docker-commands.md` — CTS's generic pre-flight only auto-discovers `rules/local/*-backend.md`/`*-angular.md` platform-split files, not this repo's full-name overrides. This repo is pure Jest (no Vitest); use Jest APIs when writing tests alongside implementation.
