## Extends .claude/agents/devops.md § "Pre-flight"

Also read `rules/local/docker-commands.md` — CTS's generic pre-flight only auto-discovers `rules/local/*-backend.md`/`*-angular.md` platform-split files, not this repo's full-name overrides.
