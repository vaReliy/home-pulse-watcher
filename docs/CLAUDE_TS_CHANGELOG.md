# claude-ts (CTS) Divergence Log

Permanent ledger of HPW's intentional or discovered divergences from the claude-ts template — bugs, gaps, or improvements found in a template-inherited file (`AGENTS.md`, `CLAUDE.md`, `rules/**`, `.claude/agents/**`, `.claude/skills/**`). Entries persist until actually ported upstream via `/cts-contribute` or `cts-import-skill`, unlike `docs/KNOWLEDGE_INBOX.md` which trends toward empty.

## Entry format

```markdown
## YYYY-MM-DD — [area] short fact

Divergence: what's different here vs. CTS, and why. Upstream action: port as-is | port with changes | HPW-only, do not port | undecided.
```

## 2026-07-01 — rules/workflow.md whole-file .ctsignore froze process content

Divergence: `rules/workflow.md` was `.ctsignore`'d wholesale during the 2026-06-12 CTS adoption to strip frontend swimlanes. That silently froze the file at its 2026-06-12 content — two rounds of generic (non-frontend) upstream improvements (Foresight gate, Nx Command Execution Policy, sequential quality gate with severity floor, `docs/CLAUDE_TS_CHANGELOG.md` knowledge-capture routing) never merged in, because a whole-file ignore doesn't distinguish "frontend-specific" from "everyone benefits." Re-merged upstream content on 2026-07-01, re-pruned frontend-only sections. Upstream action: HPW-only fix (the freeze was a local process gap, not a CTS bug). No port needed, but worth noting in `/cts-update`'s own docs that whole-file `.ctsignore` entries for large, frequently-updated files should be re-diffed periodically rather than treated as "customized forever."

## 2026-07-01 — vitest-testing skill is Vitest-only despite claiming Jest support

Divergence: CTS's `.claude/skills/vitest-testing/SKILL.md` description says "Testing with Vitest (or Jest)" but every code example uses Vitest-only APIs (`vi.fn`, `vi.mock`, `import ... from 'vitest'`) — zero Jest examples. HPW is pure Jest (no Vitest dependency anywhere). Rewrote HPW's local copy to Jest APIs (`jest.fn`, `jest.mock`, no import needed) and added it to `.ctsignore`. Upstream action: undecided — either (a) make the skill genuinely framework-agnostic (dual examples or a note on the API mapping), or (b) split into `vitest-testing` + `jest-testing`. Not done yet; revisit when contributing back via `/cts-contribute`.

## 2026-07-02 — rules/code-style.md missing a strictNullChecks relational-operator gotcha

Divergence: added a new section, "Do Not Drop `!= null` Guards Before Relational Comparisons", documenting that `strictNullChecks` rejects `>`/`<`/`>=`/`<=` directly on `T | null`/`T | undefined` operands (`TS2531`/`TS2532`) regardless of downstream reuse — discovered while fixing a Telegram battery-voltage display bug, after two incorrect draft attempts assumed JS runtime null-comparison semantics (`null > 0` === `false`) meant the guard was redundant. Verified against real `tsc` output via a `backend-developer` agent before landing the final wording. This is general strict-TS advice, not HPW-specific (no NestJS/Nx/ESP32 content) — a genuine content gap in CTS's `rules/code-style.md`, not a customization. Upstream action: port as-is via `/cts-contribute` — general TS gotcha applicable to any CTS consumer using `strictNullChecks`.

## 2026-07-09 — rules/workflow.md's Knowledge Capture "What NOT to save" list didn't guard against task-content leakage

Divergence: during Batch C's knowledge capture, a `docs/KNOWLEDGE_INBOX.md` entry was written that restated a parked task's full reasoning inline (a keep-warm-scheduler-vs-device-heartbeat tradeoff), not just a pointer to the task file — the project owner flagged this as unwanted leakage of task-tracking state into a committed doc. `rules/workflow.md`'s existing "What NOT to save" list (ephemeral task details, ephemeral state) didn't explicitly call out this specific failure mode (restating parked/open-decision reasoning specifically, as opposed to generic "task list" details), so it wasn't obviously covered until it happened. Added a bullet clarifying: parked/in-progress task reasoning must not be restated in committed docs — skip logging entirely, or at most a bare one-line path pointer. This is general knowledge-capture hygiene, not HPW-specific. Upstream action: port as-is via `/cts-contribute` — applicable to any CTS consumer using the Knowledge Inbox pattern.

## 2026-07-09 — rules/workflow.md's Execution Model had no guidance on resuming an agent across an iterative fix-retry loop

Divergence: `rules/workflow.md`'s Execution Model section already documents Claude-Code-specific dispatch mechanics (`Agent` tool, `TeamCreate`, and later a full `SendMessage`/`TeamDelete` Tool API Reference section), but had no guidance on resuming a dispatched agent across a fix-retry loop (a script fails, gets fixed, fails differently, gets fixed again). The orchestrator repeatedly spawned a fresh `Agent` call per iteration instead of resuming the same one via `SendMessage({ to: <agentId> })`, causing real context-drift bugs across iterations (a `.env.example` contradiction and a Secret-Manager-mapping mistake, both from a new agent not seeing earlier fixes to the same files) before the user flagged it. Added a bullet to Execution Model: resume the same agent for iterative same-file fix loops; only quality-gate agents (tester/reviewer/security-scanner/qa) should always get a clean un-primed look, never resumed. Upstream action: port as-is via `/cts-contribute` — general orchestrator dispatch-efficiency guidance, not HPW-specific.

## 2026-07-07 — rules/task-authoring.md has no batch-execution mechanism despite cost-tier work

Divergence: CTS cost optimizations (model tiers, conditional quality gate, severity floor, T0–T3 planning ladder) all reduce per-task cost, but `rules/task-authoring.md`'s "one task = one clean session" rule still forces a full session + quality-gate cycle per task even when several emitted tasks share files, persona, and gate composition (e.g. three firmware-security tasks all touching `ota.cpp`). HPW introduced a batch pattern locally: an `NN-00-INDEX-*-batches.md` file defining batches with an execution contract (one session per batch, one combined gate over the batch diff, shared 2-cycle restart budget, per-task acceptance criteria still checked), plus a `Context` header row on each member task pointing at the index. First instance: `tmp/tasks/todo/2026-07-07-00-INDEX-audit-batches.md`. Upstream action: port with changes via `/cts-contribute` — generalize into a "Batch execution" section of `rules/task-authoring.md` after the pattern proves itself on at least one executed batch.

## 2026-07-17 — /cts-update round-3 sync: two conflicts resolved, six customized files hand-merged

Divergence: synced to claude-ts `2a45402` (16 upstream commits) via the new guard-carrying engine (bootstrapped manually — local tooling predated the self-update-first step). Zero `BASELINE INTEGRITY` findings (no phantom-baseline holes here, unlike penny). Conflicts: `docs/METRICS.md` kept HPW's own rows (per-project ledger — upstream's claude-ts rows dropped); `rules/docker-commands.md` kept both dual-appended sections (HPW's "Docker Cleanup" + upstream's "Docker Compose Healthchecks"). Hand-merged upstream round-3 content into six `.ctsignore`'d files while preserving local customizations: `rules/workflow.md` (nx typecheck + target-name lockstep, Dispatch/report protocol, Spawn-context requirement, working-tree verification, lint scratch-demonstration, handoff-based hard stop, micro-resolution lane, Mid-pipeline transcription, Skill Renaming, Tool API rewrite removing dead `TeamCreate`/`TeamDelete`), `rules/testing.md` (`.env`-sourcing + NestJS sections, Jest-adapted; SPA serve-static subsection skipped — no frontend), `rules/code-style.md` (Shell Script Conventions: jq `//` vs shell `||`), `rules/validation-authorization.md` (validator bootstrap-registration footgun), `AGENTS.md` (indexed shell-scripting/task-authoring/nx-generators/dependencies), `CLAUDE.md` (immediate `## Learnings` transcription). Known remaining local drift, deliberate: workflow.md stays pre-tiered-ladder; testing.md still shows Vitest syntax despite pure-Jest project (see 2026-07-01 entry). Upstream action: HPW-only (this is downstream adoption, not a divergence to port) — except `rules/docker-commands.md`'s "Docker Cleanup" section: port as-is via `/cts-contribute` (generic never-`docker system prune -a --volumes` guidance).

## 2026-07-23 — migrated to two-layer CTS distribution model

Divergence: switched from the old 3-way-merge/`.ctsignore`-whole-file-fork model to the two-layer overwrite-sync model (synced to claude-ts `e563d2b` on `refactor/two-layer-distribution`, replacing `.claude/scripts/cts-sync.sh` with the new self-updating engine first, per the known stale-engine corruption bug). All 14 flat `rules/*.md` forks deleted, superseded by `rules/cts/*.md` (verbatim upstream) with genuine customizations promoted into `rules/local/{code-style,testing,validation-authorization,docker-commands}.md`. `AGENTS.md`/`CLAUDE.md`/`.claude/agents/{reviewer,tester,queue-specialist}.md` similarly reset to pure upstream, with `AGENTS.local.md`, `CLAUDE.local.md`, and `.claude/agents-local/{reviewer,tester,queue-specialist,backend-developer}.md` created to carry HPW's stack facts, caveman style, Jest-only test guidance, and single-quote code style. `.claude/settings.json` no longer needs a `.ctsignore` entry — the new engine jq-deep-merges `.cts/settings.cts.json` into it automatically; removed the now-dead `superpowers@superpowers-marketplace` plugin pin in the same pass (confirmed unused: only stale doc-text mentions in payload files remained, all now overwritten). `.ctsignore` trimmed from 18 real entries to 7 (6 pruned frontend agents/skills + `vitest-testing` skill fork, which has no lex-specialis override mechanism unlike `rules/**`/`.claude/agents/**`) — plus 2 new entries (`.prettierignore`, `docs/METRICS.md`) added _after_ the first sync overwrote them with upstream's own content, since neither had a prior `.ctsignore` entry to protect it under the old model and both carry genuine project-only data (HPW's `/dist`/`/coverage`/`/.nx/*` ignores; HPW's own per-task metrics rows). Both were restored from git history. Also discovered: CTS agent pre-flight sections only auto-discover `rules/local/*-backend.md`/`*-angular.md` platform-split files, not full-name overrides like `rules/local/code-style.md` — worked around by having the `agents-local` override files explicitly cite the relevant `rules/local/*.md` paths. Upstream action: (1) the pre-flight generic-override-discovery gap above — port as a fix widening agent pre-flight instructions to also check for a same-named `rules/local/<name>.md` whenever `rules/cts/<name>.md` is read, not just the platform-split naming pattern; (2) `vitest-testing` skill's Jest support — still just a candidate, not yet contributed (see 2026-07-01 and 2026-07-17 entries above).

## 2026-07-26 — editor formatOnSave can silently re-corrupt CTS-owned files right after a clean sync

Divergence: not a `cts-sync.sh` bug — verified by comparing `sha256sum` of the freshly-synced file against the hash `cts-sync.sh` recorded in `.cts/manifest.json` for that path (they matched the clean upstream source exactly, twice, even after a manual `cp -p` reset). Within seconds, 6 payload files (`.claude/settings.json` + 5 `.claude/skills/*/SKILL.md`) picked up a stray extra blank line each — always at the boundary where a YAML frontmatter `description: >-` folded scalar ends and the next key begins, or inside an embedded ` ```yaml ` fence right before a following block. Root cause: VS Code's `editor.formatOnSave: true` + `esbenp.prettier-vscode` as default formatter, firing when the Claude Code ↔ VS Code integration reloads/saves an already-open buffer after an on-disk write. The extension's bundled Prettier resolves independently of the repo's committed `.prettierrc` + CLI invocation, so `npx prettier --write`/`--check` reports the corrupted file as already "unchanged" — it's judging by different formatting rules than whatever the extension actually applied, so the CLI can't be used to detect or fix this. Purely local-machine editor config, would reproduce for any consumer with a similar VS Code setup and these files open during a sync. Upstream action: undecided — candidate additions to `/cts-update`'s narration step: (a) note that a synced file's diff reducing to pure whitespace/blank-line changes right after a sync (not attributable to the upstream commit range) suggests an external formatter raced the sync, and point at the `.cts/manifest.json` hash-comparison trick above as the way to confirm/rule out the engine; (b) `cts-sync.sh` could `git diff --stat` its own writes immediately post-copy (before returning) as a self-check baseline, though that wouldn't catch a race that happens after the script exits. Not yet contributed.
