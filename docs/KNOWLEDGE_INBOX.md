# Knowledge Inbox

Append-only queue for durable, project-relevant learnings whose final home isn't clear yet. Distilled into PROJECT_CONTEXT.md / CLAUDE.md / a rule / a skill, then deleted from here — this file should trend toward empty.

## 2026-09-12 — GRILL CANDIDATE: standing instructions get ignored under real task pressure, not just at append-only ledgers

Why: `docs/METRICS.md` got a new row inserted mid-table (not appended at tail) because a `Read` was truncated (`limit` param) right before the edit — recurred 4+ times same day despite an existing private memory note covering this exact failure mode ("read append-only ledgers in full before appending"). The memory note existing and being ignored is the real signal: this isn't a wording problem fixable by adding one more instruction to `rules/cts/workflow.md` (same class of fix already failed once as a memory entry). Deliberately NOT patched into workflow.md — user flagged that doing so risks becoming a pile of one-off patches for a systemic "instructions present in target file but skipped by the executor anyway" pattern that may already affect other rules (pre-flight reads, Phase 3 handoff checklist items, gate steps). Needs a `grill-with-docs` session to find a mechanical (not instructional) fix — e.g. true EOF-append via shell redirection instead of Edit-with-anchor, and/or a pre-commit check asserting diffs to the three append-only ledgers (`docs/METRICS.md`, `docs/KNOWLEDGE_INBOX.md`, `CHANGELOG.md`) only add lines at file tail — plus a broader audit for the same ignored-instruction pattern elsewhere. Interim stopgap in use: append via `printf >> file` instead of `Edit`. Belongs in (guess): grill session (not a rule patch) — do not resolve by adding text to `rules/cts/workflow.md` or any other rule file without that session first.

## 2026-09-12 — Sandboxed GCP SA can't audit its own project's IAM policy (Resource Manager perms)

Why: task asked to run `gcloud projects get-iam-policy $PROJECT_ID` to check whether the compute default SA still carries the auto-granted `roles/editor`. The only credential available in-session (`ota-manager@...`) intentionally lacks `resourcemanager.projects.getIamPolicy`, by design of the least-privilege service-account split already in place — so no agent working from this repo's normal credentials can self-verify project-level IAM state; that always needs an operator with IAM Admin rights to run the query and report back. `scripts/gcloud-bootstrap.sh` now has a defensive, idempotent `roles/editor` revoke on `RUNTIME_SA` (mirrors the existing `secretmanager.secretAccessor` revoke pattern) added without ever confirming the role was actually present — safe because the call is a no-op if the binding doesn't exist, but the "documented current bindings" acceptance criterion is still open pending a live `get-iam-policy` run post-bootstrap. Belongs in (guess): `PROJECT_CONTEXT.md`'s GCP/IAM section (already got a related edit this session) or a short note in `rules/local/*` about what operator-only steps a bootstrap/audit task can never complete from an agent session.

## 2026-09-12 — gcloud describe fail-open pattern is unsafe for destructive-action guards

Why: implementing the api-runtime-sa migration guard (`scripts/gcloud-bootstrap.sh`), first draft used `CURRENT_SA=$(gcloud run services describe ... 2>/dev/null || echo "")` to detect "service not deployed yet" before allowing an IAM revoke. This conflates "service absent" with "describe call errored for any reason" (expired auth, wrong active project, API not enabled, network blip) — both yield empty string, so an errored check on a live prod service silently fell through to "safe to revoke." Reviewer caught it; fix separates existence check (`if gcloud ... &>/dev/null; then`) from the value fetch, matching the pattern already used 3x elsewhere in the same file (~lines 459/468/491). Also: `spec.template.spec.serviceAccountName` comes back empty for a service running under the implicit default compute SA (no explicit --service-account ever set) — must not treat empty-but-exists the same as "safe," map it to the known legacy SA instead.

Belongs in (guess): `rules/local/shell-scripting.md` — general rule: any bash guard gating a destructive/irreversible action (IAM revoke, delete, etc.) must fail closed on command error, never fold "command failed" into the same branch as "expected empty/absent state." Existence checks and value fetches must be separate calls/branches.

## 2026-09-12 — platformio.ini section-scoped parsing must anchor headings to line-start

Why: fixing `test_esp32c3_dev_env_disables_tls`/`test_esp32c6_dev_env_disables_tls` (`libs/firmware-shared/test/test_transport_security/test_transport_security.cpp`) to scope their `-DHPW_USE_TLS=0` check to the `[env:esp32c3_dev]`/`[env:esp32c6_dev]` section instead of whole-file search. `firmware/esp32c3/platformio.ini` and `firmware/esp32c6/platformio.ini` have prose comments above sections that mention the _next_ section heading by name (e.g. `; ... see [env:esp32c3_dev] below ...`) before the real heading line — a plain `std::string::find(ini, "[env:esp32c3_dev]")` latches onto that comment first and extracts the wrong slice. New `extractEnvSection()` helper requires the heading match be preceded by `\n` or be at offset 0 (i.e. actually starts a line). Also noted: the pre-existing sibling `_release` tests do NOT use any section-scoping helper (still whole-file `contains(ini, "-DHPW_USE_TLS=1")`) — a prior task file incorrectly assumed they did; they may deserve the same fix as a follow-up.

Belongs in (guess): `libs/firmware-shared` test-writing conventions (rule or comment near the helper) — any future platformio.ini section-scoped test parsing must reuse `extractEnvSection` or its anchoring approach, not re-derive a naive `find`.

## 2026-09-12 — Debug-log PII redaction: fix at call site, do not trust log-level default

Why: auditing `logger.debug()` calls for PII leaks (`apps/api/src/modules/telegram/telegram.controller.ts` logged raw Telegram message text), found the repo has no `LOG_LEVEL` env var and no explicit `level` in the main API's `pinoHttp` config (`app.module.ts`) — Pino defaults to `info`, so `.debug()` never emits today. Chose redaction (log `textLength` instead of raw text) over an opt-in env-var gate, since adding new env-var infra just to gate one log line would be dead weight when the default already suppresses it — but redaction is still correct because the default is one config line away from being flipped to `debug`, and a future dev flipping it for troubleshooting must not accidentally leak PII. Separately, `apps/api/src/cli/cli.module.ts:28` sets its own `LoggerModule.forRoot({ pinoHttp: { level: 'warn' } })`, independent of the API app's config — worth checking both if auditing log levels again.

Belongs in (guess): `PROJECT_CONTEXT.md`'s Structured Logging section (already got a related note this session) — the general principle ("redact PII at the log call site, never rely on log-level defaults as a security boundary") could also become a short line in `rules/cts/validation-authorization.md` or a security rule if a second occurrence shows up.

## 2026-09-12 — pino-http autoLogging serializes req.headers by default at base log level, not just debug

Why: security-scanner independently re-auditing the PII debug-log fix found that `nestjs-pino`/`pino-http`'s default request serializer includes the full `req.headers` object in every request log line at whatever the base log level is (`info` here, in every env) — not gated behind `.debug()` calls at all. `apps/api/src/app/app.module.ts:33-58` has no `redact` config, so `X-Device-Mac`/`X-Signature`/`X-Timestamp` (HMAC guard) and `x-telegram-bot-api-secret-token` (Telegram webhook static secret) are logged on every request. This is a distinct leak surface from explicit `.debug()` call-site auditing — a debug-log audit must also check the HTTP logging middleware config, since autoLogging happens outside application code and won't show up in a `.debug(`/`.verbose(` grep. Follow-up task filed: `tmp/tasks/todo/2026-09-12-15-redact-auth-headers-in-http-logs.md`.

Belongs in (guess): `PROJECT_CONTEXT.md`'s Structured Logging section (add "auth headers must be in pino redact config") once the follow-up task lands; could also become a line in a security/logging rule for future audits to check middleware config, not just call sites.

## 2026-09-12 — docker-compose postgres volume keeps stale password across restarts without -v

Why: implementing the `boardType` migration (`tmp/tasks/todo/2026-09-12-13-add-boardtype-to-device.md`), local `prisma migrate dev` failed with `P1000: password authentication failed` against localhost:5432. Root cause: the `home-pulse-watcher_postgres_data` docker volume persists across `docker compose down` (without `-v`), so `POSTGRES_PASSWORD` only takes effect on first initdb — restarting with a different `DB_PASSWORD` against an existing volume silently keeps the old password baked into the volume, producing a misleading auth-failure error rather than any volume-mismatch signal. Fix: `docker compose down -v postgres \&\& DB_PASSWORD=<anything> docker compose up -d postgres` gives a clean known-credential local DB (safe for local/dev only — throwaway data, not prod).

Belongs in (guess): a short note near the local Docker/Postgres setup instructions (README or `rules/local/docker-commands.md`) so future agents dont waste a debug cycle assuming DB auth failure means wrong `.env` value.
