# Knowledge Inbox

Append-only queue for durable, project-relevant learnings whose final home isn't clear yet. Distilled into PROJECT_CONTEXT.md / CLAUDE.md / a rule / a skill, then deleted from here — this file should trend toward empty.

## 2026-09-12 — GRILL CANDIDATE: standing instructions get ignored under real task pressure, not just at append-only ledgers

Why: `docs/METRICS.md` got a new row inserted mid-table (not appended at tail) because a `Read` was truncated (`limit` param) right before the edit — recurred 4+ times same day despite an existing private memory note covering this exact failure mode ("read append-only ledgers in full before appending"). The memory note existing and being ignored is the real signal: this isn't a wording problem fixable by adding one more instruction to `rules/cts/workflow.md` (same class of fix already failed once as a memory entry). Deliberately NOT patched into workflow.md — user flagged that doing so risks becoming a pile of one-off patches for a systemic "instructions present in target file but skipped by the executor anyway" pattern that may already affect other rules (pre-flight reads, Phase 3 handoff checklist items, gate steps). Needs a `grill-with-docs` session to find a mechanical (not instructional) fix — e.g. true EOF-append via shell redirection instead of Edit-with-anchor, and/or a pre-commit check asserting diffs to the three append-only ledgers (`docs/METRICS.md`, `docs/KNOWLEDGE_INBOX.md`, `CHANGELOG.md`) only add lines at file tail — plus a broader audit for the same ignored-instruction pattern elsewhere. Interim stopgap in use: append via `printf >> file` instead of `Edit`. Belongs in (guess): grill session (not a rule patch) — do not resolve by adding text to `rules/cts/workflow.md` or any other rule file without that session first.

## 2026-09-12 — Sandboxed GCP SA can't audit its own project's IAM policy (Resource Manager perms)

Why: task asked to run `gcloud projects get-iam-policy $PROJECT_ID` to check whether the compute default SA still carries the auto-granted `roles/editor`. The only credential available in-session (`ota-manager@...`) intentionally lacks `resourcemanager.projects.getIamPolicy`, by design of the least-privilege service-account split already in place — so no agent working from this repo's normal credentials can self-verify project-level IAM state; that always needs an operator with IAM Admin rights to run the query and report back. `scripts/gcloud-bootstrap.sh` now has a defensive, idempotent `roles/editor` revoke on `RUNTIME_SA` (mirrors the existing `secretmanager.secretAccessor` revoke pattern) added without ever confirming the role was actually present — safe because the call is a no-op if the binding doesn't exist, but the "documented current bindings" acceptance criterion is still open pending a live `get-iam-policy` run post-bootstrap. Belongs in (guess): `PROJECT_CONTEXT.md`'s GCP/IAM section (already got a related edit this session) or a short note in `rules/local/*` about what operator-only steps a bootstrap/audit task can never complete from an agent session.
